for sample in sampleList:
    sampleDict[sample] = 

maybe just remove the entire platedict, we can sort into sampledict right away instead of having the intermediate dictionary

each sample is in format:
sampleDict[sampleNum] = (adate, [[row1][row2][row3][row4]]
